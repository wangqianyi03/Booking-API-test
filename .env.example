# 复制本文件为 .env 后按本机环境修改。
# .env 不会进 Git。AUTH_USERNAME / AUTH_PASSWORD 必填，程序不会在代码里兜底。
# 下面是 Restful Booker 公开演示账号，仅供本地练习。

TEST_ENV=local
BASE_URL=http://localhost:3001
AUTH_USERNAME=admin
AUTH_PASSWORD=password123
LOG_LEVEL=INFO
